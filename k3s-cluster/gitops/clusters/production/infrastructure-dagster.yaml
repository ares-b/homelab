apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-dagster
  namespace: flux-system
spec:
  interval: 10m
  retryInterval: 2m
  # Needs the dagster database + role from the postgresql cluster.
  dependsOn:
    - name: infrastructure-postgresql
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./k3s-cluster/gitops/infrastructure/apps/dagster
  prune: true
  wait: true
  timeout: 15m
