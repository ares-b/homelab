apiVersion: v1
kind: Namespace
metadata:
  name: postgresql
  annotations:
    # Stateful namespace: never let Flux prune it (e.g. during Kustomization moves).
    kustomize.toolkit.fluxcd.io/prune: disabled
