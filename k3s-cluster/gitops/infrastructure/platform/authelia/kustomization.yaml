apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
  - middleware.yaml
  - ingressroute.yaml
  - sealed-authelia-users.yaml
  - sealed-authelia-smtp.yaml
  - sealed-authelia-oidc.yaml

configMapGenerator:
  - name: authelia
    namespace: authelia
    files:
      - configuration.yml
