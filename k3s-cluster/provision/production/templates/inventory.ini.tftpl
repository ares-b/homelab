[k3s]
%{ for name, node in nodes ~}
${name} ansible_host=${split("/", node.ip)[0]}
%{ endfor ~}

[k3s_server]
%{ for name, node in nodes ~}
%{ if node.role == "server" ~}
${name}
%{ endif ~}
%{ endfor ~}

[k3s:vars]
ansible_user=ops
ansible_ssh_common_args=-o StrictHostKeyChecking=accept-new
