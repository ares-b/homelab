---
# Host config, run via `make -C k3s-cluster configure`. Cluster objects
# (users, node labels) are separate Terraform roots under ../../resources.
- import_playbook: node-prep.yml
- import_playbook: disk-setup.yml
- import_playbook: firewall.yml
