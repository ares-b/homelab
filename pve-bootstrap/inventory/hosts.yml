---
all:
  children:
    proxmox_nodes:
      hosts:
        pve01:
      vars:
        ansible_connection: ssh
        ansible_user: ansible
        ansible_ssh_private_key_file: /tmp/ansible_pve_key
        ansible_become: true
        ansible_become_method: sudo
